# === Feature flags (OFF par défaut) ===
FEATURE_CURRICULUM=0
FEATURE_QUIZ=0
FEATURE_RAG=0
FEATURE_ADMIN=0

# === Auth / JWT ===
JWT_SECRET=change-me

# === DB (placeholders uniquement; encoder le mot de passe si nécessaire) ===
POSTGRES_PASSWORD=change-me
DATABASE_URL=postgresql://nsi:CHANGE_ME@pgbouncer:5432/nsi?pgbouncer=true&connection_limit=1
DIRECT_URL=postgresql://nsi:CHANGE_ME@postgres:5432/nsi
SHADOW_DATABASE_URL=postgresql://nsi:CHANGE_ME@postgres:5432/nsi

# === Runners / Workers ===
PY_RUNNER_URL=http://runner-py:8000
REDIS_URL=redis://redis:6379

# === S3 (placeholders) ===
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=
S3_SECRET_KEY=
S3_BUCKET=reports
S3_REGION=us-east-1
S3_FORCE_PATH_STYLE=true

# === LLM / Embeddings (invariants) ===
GEMINI_API_KEY=
VECTOR_DIM=768

# === App routing (si utilisé) ===
NEXT_BASE_PATH=
APP_BASE_URL=
